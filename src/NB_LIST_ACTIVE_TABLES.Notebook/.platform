{
  "$schema": "https://developer.microsoft.com/json-schemas/fabric/gitIntegration/platformProperties/2.0.0/schema.json",
  "metadata": {
    "type": "Notebook",
    "displayName": "NB_LIST_ACTIVE_TABLES",
    "description": "Returns active ingestion.Table rows (joined to Database/Connection), each with a fully resolved, ready-to-run SourceQuery (watermark-bounded for LoadType='Delta'), for a given connector ConnectionType as a JSON exit value, so PL_INGEST_SQL/PL_INGEST_FILE can ForEach over them without the pipeline needing its own connection to the metadata catalog."
  },
  "config": {
    "version": "2.0",
    "logicalId": "8b8ec189-0042-4111-89dd-9a3f8ea444d9"
  }
}
